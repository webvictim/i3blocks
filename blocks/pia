#!/bin/bash

VPN_STATUS=$(nmcli con show id "PIA - London" | grep VPN-STATE | grep -q "VPN connected")
if [ $? -eq 0 ]; then
    VPN_STATUS="PIA on"
else
    VPN_STATUS="PIA off"
fi

echo "${VPN_STATUS}"
echo "${VPN_STATUS}"
echo ""
